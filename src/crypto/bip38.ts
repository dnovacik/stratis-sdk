// import asset from 'assert'
// import * as bcrypto from 'bcrypto'
// import xor from 'buffer-xor'
// import crypto from 'crypto'

// import { fromPrivateKey } from './../identity/keys'

// const NULL = Buffer.alloc(0)

// const getPublicKey = (buffer: Buffer, compressed: boolean): Buffer => {
//   return Buffer.from(fromPrivateKey(buffer, compressed).publicKey, 'hex')
// }